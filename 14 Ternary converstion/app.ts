// # make a Typescript file. convert this code into ternary statment
// ## Task 1:
let temperature = 25; 
let summer = temperature ? "It's too hot outside!" : "It's not too hot outside.";
if (temperature > 30) {
    console.log(`It's too hot , ${temperature} temperature outside!`);
} else {
    console.log(`It's not too hot, ${temperature} tempearature outside.`);
}
